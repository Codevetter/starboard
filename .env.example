# GitHub OAuth (NextAuth)
GITHUB_ID=
GITHUB_SECRET=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Turso database
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# AI Gateway (embeddings for semantic search)
AI_GATEWAY_URL=https://free-ai-gateway.sarthakagrawal927.workers.dev
AI_GATEWAY_API_KEY=

# Weekly digest email (scripts/send-weekly-digest-emails.ts — fail-closed:
# delivery is skipped with a log when RESEND_API_KEY is unset)
RESEND_API_KEY=
# Optional verified Resend sender, e.g. "Starboard <digest@example.com>"
DIGEST_EMAIL_FROM=
