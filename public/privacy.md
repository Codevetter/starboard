# Starboard privacy

Public discovery uses a shared seeded GitHub repository corpus. Personal stars, connected projects, tags, collections, comments, and votes require GitHub authentication and remain scoped to the signed-in user unless a list is explicitly shared.
